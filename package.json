{
  "name": "web-rpg",
  "private": true,
  "version": "0.1.0",
  "description": "Mobile-first web RPG monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.base.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

